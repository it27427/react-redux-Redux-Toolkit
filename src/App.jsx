import { BrowserRouter, Routes, Route } from 'react-router-dom';

const App = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route element={} />
      </Routes>
      <h1>React Redux - Redux Toolkit</h1>
    </BrowserRouter>
  );
};

export default App;
